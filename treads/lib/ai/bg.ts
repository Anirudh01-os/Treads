export async function removeBackground(inputUrl: string): Promise<{ outputUrl: string }>{
  // Placeholder: integrate Replicate or HF model for background removal
  // Return the same URL for now to keep flow working
  return { outputUrl: inputUrl };
}

